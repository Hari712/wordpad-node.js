version: 2.1
orbs:
  node: circleci/node@3.0.0


workflows:
  node-tests:
    jobs:
      - node/test
  build:
      - run:
          name: Run Tests
          command: npm run test
      jobs:
        - build